# 小小榆项目全面优化提示词（可直接复制使用）

> 用法：将下方「提示词正文」整段复制，粘贴给 Claude Code / Cursor / ChatGPT 等 AI 或开发团队使用。
> 版本：v1.0 ｜ 生成日期：2026-09-18 ｜ 适用项目：E:\ai-chat-desktop（小小榆 v3.0.2 多模型 AI 桌面助手）

---

# 提示词正文（从这里开始复制）

## 一、你的角色与任务

你是资深的全栈架构师兼产品设计师。请对位于 `E:\ai-chat-desktop` 的「小小榆」项目进行一次**系统性、可落地的全面优化**，覆盖 **前端（Electron 渲染层）、后端（Electron 主进程 + Express Web 服务）、Python 训练链路、打包发布与工程质量** 全部层面。

优化原则：

1. **不破坏现有功能**：所有改动必须以现有功能正常运行为前提，逐模块小步改造，每完成一块就验证一块。
2. **可审计**：每个改动点说明「改了什么文件、为什么改、影响范围」。
3. **按优先级推进**：先 P0（设计系统与核心体验），再 P1（后端稳定性与安全），最后 P2（工程化与打磨）。
4. 所有代码改动保持项目现有技术栈（Electron 33 / React 18 / TS 5.6 / Ant Design 5 / Zustand / Express 5 / sql.js），**不引入重框架、不无谓增加依赖**。

---

## 二、项目背景（供你理解现状）

- 产品定位：本地优先的多模型 AI 桌面助手（Windows/macOS），内置 Ollama 本地模型（qwen2.5:0.5b），支持 DeepSeek-R1 微调、多 API 适配器（Anthropic / ERNIE / Gemini / OpenAI-compat）、技能市场、MCP、Web 版（Express + JWT + 验证码）。
- 核心目录结构：
  - `src/main/` — Electron 主进程：窗口、托盘、全局快捷键、Ollama 后台初始化（`ollama/ollama-launcher.ts`）、自动更新（`updater/auto-updater.ts`）、IPC（`ipc/*.ipc.ts`）、本地数据库（`store/database.ts` + crypto 加密）
  - `src/server/` — Express Web 服务：JWT 认证、登录限流、SSE 聊天流、会话/提供商/设置/技能路由
  - **认证现状与目标**：当前仍为「用户名 + 密码登录、用户名 + 图形验证码注册」（`src/server/routes/auth.ts` + `src/renderer/components/auth/LoginPage.tsx`，PBKDF2 哈希 + JWT 30 天）；**已定目标（改造进行中，尚未落地）**：登录改为 **QQ 邮箱 + 密码**，注册改为 **QQ 邮箱 + 邮箱验证码**。本次优化须把该改造落地并补齐安全设计。
  - `src/adapters/` — 多模型适配器层（统一超时、SSE 流式合并、错误标准化）
  - `src/renderer/` — React 前端：`components/layout/`（AppLayout/Sidebar/TitleBar）、`components/chat/`（ChatView/MessageBubble/InputArea/WelcomeView）、`components/settings/`、`components/workspace/`、`components/finetune/`、`stores/`（Zustand）、`styles/`（global.css + modern.css 设计 Token + 暗色模式）
  - `src/api/` — 渲染层传输层（electron IPC / HTTP 双通道）
  - `python/` — QLoRA 微调（train.py / export.py / setup.py）
  - 打包：electron-builder（Windows NSIS / macOS dmg+zip）

---

## 三、优化总目标

| 维度   | 目标                                               |
| ---- | ------------------------------------------------ |
| 前端体验 | 达到 iOS 原生质感：高级玻璃（Liquid Glass）设计系统，全组件统一、明暗双模式完美 |
| 后端质量 | 主进程职责清晰、IPC 安全可控、Web 服务健壮，错误可诊断                  |
| 性能   | 启动 < 3s，UI 响应 < 100ms，内存 < 2GB（不含模型），长列表与流式输出流畅  |
| 工程   | 类型安全（消灭 any）、关键路径有测试、文档与代码同步                     |

---

## 四、前端优化 —— iOS 高级玻璃质感（最高优先级）

目标：把「小小榆」的视觉与交互整体升级为 **iOS 原生质感的高级玻璃设计（Liquid Glass / Glassmorphism）**——半透明分层、背景模糊、高光描边、柔和光效，像 macOS Big Sur 之后的系统界面与 iOS 通知中心。

### 4.1 设计 Token 升级（styles/modern.css 为主，global.css 配合）

重写 `:root` 设计变量，建立「玻璃设计系统」：

- **玻璃表面 Token**：
  - `--glass-bg`（浅色：`rgba(255,255,255,0.55~0.65)`；暗色：`rgba(30,32,40,0.55~0.65)`）
  - `--glass-bg-strong`（浮层/弹窗用，透明度更低更实）
  - `--glass-bg-subtle`（面板内嵌层，透明度更高）
  - `--glass-blur`（`blur(24px) saturate(180%)` 等三档：面板 20px / 浮层 32px / 全屏 40px）
  - `--glass-border`（高光描边：顶部 `rgba(255,255,255,0.5)` 渐变到 `rgba(255,255,255,0.08)`；暗色用 `rgba(255,255,255,0.12)`）
  - `--glass-shadow`（多层柔和阴影：环境光 + 主光，iOS 漫反射风格）
  - `--glass-noise`（极低透明度噪点叠加层，消除渐变 banding）
- **iOS 系统色**：主色建议由 `#7C3AED` 调整为 iOS 风格蓝 `#0A84FF`（暗色）/ `#007AFF`（亮色），保留品牌紫色作为点缀（如渐变、强调图标）；功能色对齐 iOS：绿 `#34C759`、橙 `#FF9500`、红 `#FF3B30`、青 `#5AC8FA`。
- **圆角体系**：升级为 iOS 大圆角——卡片 20px、容器 16px、控件 12px、胶囊 999px；弹窗与侧边栏用更接近 squircle（连续曲率）的观感。
- **字体**：字体栈统一为 `-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Microsoft YaHei", sans-serif`；建立 iOS 字号层级（正文 15px、次级 13px、标题 17px、大标题 22px 加粗）。
- **阴影**：重做阴影 Token 为柔和多层（如 `0 1px 1px rgba(0,0,0,0.02), 0 4px 8px rgba(0,0,0,0.04), 0 16px 32px rgba(0,0,0,0.06)`），避免生硬投影。

### 4.2 玻璃材质规范（全局 CSS 工具类）

新增一组可复用的玻璃类（放 modern.css）：

```
.glass-panel      /* 主表面：半透明 + blur + 高光描边 + 阴影 */
.glass-panel-strong  /* 弹窗/抽屉浮层 */
.glass-inset      /* 内嵌凹陷表面（输入区、列表项） */
.glass-hairline   /* 1px 高光分隔线 */
.glass-noise      /* 噪点纹理层（::after 叠加） */
```

- 所有玻璃表面统一用 `backdrop-filter: blur(...) saturate(180%)`，并配合 `background: linear-gradient` 制造顶部高光。
- **背景画布层**：全局加一个底层「氛围画布」（动态渐变 + 微光斑，暗色为深海渐变），玻璃元素必须能"透"到它，否则玻璃感不成立。背景可跟随用户设置的 `bgImage`（现有 settings 已有 bgImage/bgOpacity），玻璃透出壁纸。
- 性能约束：`backdrop-filter` 不叠加嵌套超过 2 层；大面积区域限制 blur 范围；必要时用 `will-change`；低端机提供降级（纯半透明无 blur）。

### 4.3 组件级改造清单（逐个落实）

| 组件                                  | iOS 化要求                                                                                                                                  |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| **AppLayout / WorkspaceLayout**     | 三层结构：底部氛围画布 → 玻璃侧边栏 → 玻璃内容区；侧边栏与内容区之间用 hairline 分隔；折叠动画用 iOS 曲线                                                                          |
| **Sidebar**                         | 毛玻璃 Sider（`glass-panel`），导航项选中态用 iOS 系统蓝半透明胶囊高亮 + 毛玻璃压感；图标统一线性风格、24px 尺寸                                                                 |
| **TitleBar**                        | 无边框窗口自绘标题栏改为玻璃材质，与侧边栏同色透；窗口控制按钮（最小化/最大化/关闭）hover 用 iOS 色（橙/绿/红）保留现有创意设计                                                                  |
| **ChatView / MessageList**          | 消息区用透明底，透出氛围画布；滚动条 iOS 化（细、半透明、自动隐藏）                                                                                                     |
| **MessageBubble**                   | 用户气泡：iOS 蓝色渐变（`#0A84FF → #007AFF`）+ 白字 + 大圆角（16-20px）；AI 气泡：`glass-inset` 半透明玻璃 + hairline 描边；头像圆形 + 渐变                                  |
| **InputArea**                       | iOS 输入栏：玻璃内嵌圆角容器、focus 时高光描边 + 轻微上浮、语音/发送按钮圆形玻璃钮、按住发送有按压反馈（scale 0.97）                                                                   |
| **WelcomeView**                     | 大标题 + 玻璃功能卡片（3 个免费模型卡片玻璃化，hover 上浮 + 高光跟随）；问候语渐变文字                                                                                       |
| **SettingsDrawer / Modal / Drawer** | 浮层统一 `glass-panel-strong`（32px blur），背后内容轻微虚化；设置项分组卡片玻璃化                                                                                 |
| **Announcement / 更新弹窗 / Ollama 进度** | 全部玻璃浮层 + iOS 风格进度条（胶囊、半透明轨道）                                                                                                             |
| **FinetuneView / TrainingPanel**    | 训练面板玻璃卡片化；进度条 iOS 风格；日志区等宽字体但容器玻璃化                                                                                                       |
| **登录页 LoginPage**                   | 全屏氛围画布 + 居中玻璃登录卡片（高光描边 + 大圆角 + 阴影），品牌 Logo 玻璃质感图标；表单同步改为 **QQ 邮箱 + 密码登录 / QQ 邮箱 + 邮箱验证码注册**（邮箱输入框、密码框、「发送验证码」按钮带 60s 倒计时、验证码输入框），全部控件玻璃化 |
| **Ant Design 组件覆写**                 | 用 ConfigProvider token + CSS 变量统一覆写：Button（胶囊/大圆角）、Input、Select、Switch（iOS 风格开关）、Tooltip（玻璃气泡）、Dropdown、Scrollbar、Tag（半透明胶囊）             |

### 4.4 动效与交互规范（iOS 手感）

- 全局过渡曲线：**iOS 弹簧曲线** `cubic-bezier(0.34, 1.56, 0.64, 1)`（弹出）与 `cubic-bezier(0.25, 0.1, 0.25, 1)`（标准 iOS）；时长 200–400ms。
- 页面/视图切换（chat ↔ finetune ↔ workspace）：横向滑动 + 淡入（iOS 导航转场）。
- 按压反馈：可点元素按下 `scale(0.98)` + 透明度 0.85，松开回弹。
- 列表项：hover 高亮、点击涟漪（iOS 风格，非 Material 重水波）。
- 弹窗：从中心/底部弹簧弹出 + 背景虚化淡入；关闭时淡出。
- 输入框：focus 高光描边扩散动画；发送按钮发送成功时轻微回弹。
- 流式输出：打字光标、AI 回答中「思考中」使用 iOS 风格三点脉冲动画（现有 ThinkingBlock 升级）。
- 窗口缩放/折叠：侧边栏折叠用 iOS 曲线 + 内容区平移。

### 4.5 明暗双模式

- 玻璃 Token 必须同时给出 light / dark 两套（暗色：更低透明度 + 更冷色调 + 更亮高光描边），跟随系统 + 手动切换逻辑保持现状。
- 暗色玻璃饱和度控制：`saturate(140%)` 左右，避免霓虹感；背景氛围画布暗色用深蓝黑渐变（呼应"深海鱼"品牌）。

### 4.6 技术实现约束

- Electron 33（Chromium 内核）支持 `backdrop-filter`，直接可用；主窗口若未开启则确认 `transparent`/`frame:false` 配置与自绘标题栏协调。
- 优先在 CSS 层完成，避免为每个组件重写 JSX；antd 用 token 覆盖。
- 改造过程中保持 i18n（zh-CN/en-US）文案不变。

---

## 五、后端优化（Electron 主进程 + Express Web 服务）

### 5.1 架构与代码组织

- 主进程瘦身：将 `main/index.ts` 中的 Ollama 初始化流程（检测→启动→拉模型→导入）抽成独立 service（可先落 `src/main/services/ollama-service.ts`），入口只做编排；同样拆分自动更新 service。保持 IPC 层薄、service 层厚。
- IPC 统一规范：所有 `ipc/*.ipc.ts` 的 handler 统一：入参类型化校验（不信任 renderer 输入）、返回统一 `{ ok, data | error }` 结构、错误带错误码（如 `OLLAMA_NOT_INSTALLED` / `MODEL_NOT_READY` / `INVALID_INPUT`），renderer 可据此给用户友好提示。
- 敏感操作鉴权：涉及文件读写、删除、训练启动等 IPC，校验来源与参数（路径白名单/前缀校验，防路径穿越）。
- Web 服务：路由按域收敛，`server/routes/*` 统一挂载 `/api` 前缀与错误中间件；SSE 路由（chat）与普通 JSON 路由拆分清晰；加入统一请求日志中间件（方法/路径/耗时/状态码）。

### 5.2 错误处理与日志

- 全链路错误边界：主进程全局 `uncaughtException` / `unhandledRejection` 兜底（记录并尝试恢复，不静默崩溃）；渲染层 React ErrorBoundary 包裹主要视图。
- 日志升级：`logger` 增加级别过滤与**日志轮转**（按天/大小切割，默认保留 7 天），训练日志、Ollama 日志、Web 请求日志分文件；日志落盘目录保持 `%APPDATA%\小小榆\logs`。
- 用户可读错误：后端所有错误最终映射为「用户能看懂的中文提示 + 排查建议」，杜绝裸抛堆栈到界面。

### 5.3 安全加固

- 密钥与凭据：API Key 等敏感配置改用 Electron `safeStorage`（系统凭据链）加密存储，替换/并存现有 crypto 方案；确认 PBKDF2 迭代、随机盐逻辑不被削弱。
- Web 端：确认 CORS 白名单、JWT 过期与刷新策略（短期 access + 可选的 refresh）、验证码一次性、登录限流（现有 20 次/分，建议失败计数递增封禁策略）、请求体大小限制；补充 CSP 响应头（renderer 生产模式）。
- 依赖安全：执行 `npm audit`，修复高危漏洞；`electron-builder` 打包开启代码签名（Windows 至少配置签名入口、macOS 维持 hardenedRuntime + 公证流程）。
- 上传/文件接口（file.ipc / workspace.ipc）：大小限制、类型白名单、路径规范化，防任意文件读写。

### 5.4 性能与稳定性

- 本地数据库：sql.js 是同步 API，主进程读写在数据量大时会卡 UI —— 评估将数据库读写迁移到 Worker 线程或改异步存储；为常用查询建索引（会话列表、消息按会话查询）。
- Ollama 链路：模型列表、状态检测加缓存与节流（避免频繁轮询）；SSE 聊天加心跳与断线自动重连提示；适配器层统一超时（120s 已有）与重试策略，流式输出 buffer 不丢尾包。
- 启动优化：保持后台懒初始化 Ollama；窗口首帧渲染完成即显示；检查 `vite` 构建产物分包（vendor 拆分），减少首屏 JS 体积。
- 长会话性能：消息列表虚拟滚动（消息量大时），避免整段 DOM 渲染；历史消息分页加载。

### 5.5 数据与状态

- 渲染层 stores（chat/conversation/model/settings/workspace）：统一数据流，避免组件内散落 useState 管理全局态；持久化策略明确（localStorage / 数据库 / 配置文件各归其位）。
- 双通道一致性：`src/api/` 的 IPC 与 HTTP 两套 transport 保持接口签名一致，Web 版与桌面版行为对齐。

### 5.6 认证体系改造 —— QQ 邮箱登录 / 邮箱验证码注册（进行中，须落地）

目标：把 `src/server/routes/auth.ts` 与 `LoginPage.tsx` 从「用户名 + 密码 + 图形验证码」改为 **QQ 邮箱 + 密码登录**、**QQ 邮箱 + 邮箱验证码注册**，同时兼容旧数据。

- **数据模型**：`users` 表以 `email`（QQ 邮箱）为登录标识（新增字段或迁移）；旧用户名数据提供迁移策略（如允许一次「绑定邮箱」或引导重置），不得丢用户。
- **邮箱校验**：QQ 邮箱格式正则（如 `^[a-zA-Z0-9._%+-]+@qq\.com$`），大小写归一化（QQ 邮箱不区分大小写，统一小写存储）。
- **注册（邮箱验证码）**：
  - 新增「发送验证码」接口：生成 6 位数字码，`crypto.randomInt` 生成，哈希后存储（防拖库），有效期 5–10 分钟，一次性消费（用过即失效）；
  - 发送通道：QQ 邮箱 SMTP（`smtp.qq.com:465`，SSL，需授权码），发件邮箱 + 授权码存 `settings` 表并**加密**（safeStorage 或现有 crypto），授权码不落明文日志；
  - 频率限制：同一邮箱 60s 内限 1 次、每日限 N 次；同一 IP 限流；验证码错误累计 5 次立即作废并重新生成；所有限制有中文友好提示。
- **登录（QQ 邮箱 + 密码）**：邮箱唯一匹配用户，PBKDF2 校验逻辑保留；保留失败次数递增封禁策略（现有登录限流升级）；不存在「该邮箱未注册」与「密码错误」以外的枚举差异（防撞库：统一提示）。
- **密码重置（建议一并实现）**：邮箱验证码 + 新密码，复用注册验证码链路。
- **日志脱敏**：日志中邮箱脱敏（如 `12***@qq.com`）。
- **前端联动**：LoginPage 表单字段、IPC/HTTP 参数、错误文案、i18n（zh/en）同步更新。

---

## 六、Python 微调与模型链路（python/）

- `train.py`：补全并验证训练主流程（模型加载、LoRA 配置、训练循环、保存导出），修复已知的路径截断/中文路径问题；输出**结构化进度**（JSON 行，含 epoch/loss/eta/显存），供 IPC 解析推送给前端进度条。
- 健壮性：显存不足时自动降级建议（降 rank / 换小模型）并友好报错；训练结束显式 `torch.cuda.empty_cache()`；支持断点续训（任务目录记录 checkpoint）；训练进程取消/退出时干净终止并释放资源。
- `export.py` / `export-model.ps1`：导出到 Ollama 的流程参数化、幂等（重复导出覆盖确认）。
- 任务队列：支持同时创建多个训练任务、顺序执行、后台训练（最小化到托盘继续跑）。

---

## 七、全链路工程化

### 7.1 性能指标（以可测方式落地）

| 指标                  | 目标       | 验证方式  |
| ------------------- | -------- | ----- |
| 应用启动到可用             | < 3s     | 计时日志  |
| UI 操作响应             | < 100ms  | 交互实测  |
| 内存占用（不含模型）          | < 2GB    | 任务管理器 |
| 首屏渲染                | 窗口显示即可交互 | 实测    |
| 100 条数据训练（RTX 4060） | < 15 分钟  | 实测    |

### 7.2 打包与发布

- 审视 `asar: false` 配置：评估开启 asar（除 `sql.js` 等需 unpack 的模块）以减小体积、防篡改；`node_modules/sql.js` 保持 asarUnpack。
- 自动更新：验证 `auto-updater.ts` 的更新源可用性、下载进度、失败回滚提示；发布前确认 Windows 安装包（NSIS）与 macOS 包的正确性（现有 `pack:mac` 流程）。
- 产物体积：检查 release 包内容，去除冗余（node_modules 中未使用依赖、多余资源）。

### 7.3 测试

- 补关键单测：适配器层（openai-compat 流式合并、超时）、crypto（加解密往返）、限流逻辑、路径安全校验。
- IPC 集成测试：核心链路（chat 对话、Ollama 状态、finetune 启动/进度）至少冒烟覆盖。
- 现有 `_test/` 与 `scripts/` 中的测试/诊断脚本：跑通并纳入 npm scripts（如 `npm test`）。
- 前端：对玻璃设计改造后的关键组件做渲染冒烟（可选，至少保证改造后构建无错、无回归）。

### 7.4 文档

- 更新 README / 文档体系：新增「架构说明」「设计系统说明（玻璃 Token 与用法）」「API/IPC 接口说明」。
- CHANGELOG：把本次优化按「前端视觉 / 后端稳定性 / 工程化」分组记录到应用内公告（AppLayout 的 CHANGELOG 数组）与仓库文档。

---

## 八、验收标准（逐项检查后才能宣布完成）

**前端**

- [ ] 亮/暗两种模式下的玻璃质感完整且一致（侧边栏、聊天、设置、弹窗、登录页全覆盖）
- [ ] 所有弹窗/抽屉为毛玻璃浮层，背景正确虚化
- [ ] 全局动效符合 iOS 曲线，按压反馈存在，无生硬跳变
- [ ] 无大面积 backdrop-filter 性能卡顿（滚动流畅）
- [ ] 字号/颜色/间距遵循新 Token，无残留旧色值（grep 抽查）
- [ ] 中文/英文 i18n 文案未丢失

**后端**

- [ ] 所有 IPC 入参有校验，错误返回统一结构
- [ ] 日志分级 + 轮转生效；全局异常有兜底
- [ ] 敏感凭据使用 safeStorage（或等价系统级加密）
- [ ] **QQ 邮箱认证改造完成**：登录仅接受 QQ 邮箱+密码；注册走邮箱验证码；验证码一次性、限频、过期；旧用户数据可迁移，无数据丢失
- [ ] 聊天 SSE、Ollama 状态、微调进度在桌面版与 Web 版均正常

**工程**

- [ ] `npm run build`（renderer + main + server）全部通过，无 TS 报错
- [ ] `npm audit` 无高危漏洞（或已记录豁免理由）
- [ ] 关键路径测试通过
- [ ] 打包产物（Windows 至少 `pack:dir`）可启动、可对话

---

## 九、硬性约束（禁止事项）

1. 不删除/重构现有用户数据存储格式（迁移需兼容旧数据）。
2. 不改动现有 API 适配器对外协议与模型配置格式，除非同时提供迁移。
3. 不引入新的重量级框架（如 Next.js、Redux Toolkit、Tailwind 全量替换等）；antd 与现有依赖保留。
4. 不破坏现有免登录游客模式、Ollama 自动初始化、技能市场、MCP、自动更新等功能。
5. 不在未验证的情况下声称完成：每项改动必须实际构建/运行验证。
6. 涉及删除、覆盖、移动用户文件的操作，必须先说明并确认。

---

**最后自检**：以上九节是否每一条都落到具体文件/组件/接口？如果某条你无法定位到代码，请先探查项目再动手，不要凭经验猜测。

# 提示词正文（到这里结束）
