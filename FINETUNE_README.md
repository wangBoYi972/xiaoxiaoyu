# 小小榆大模型微调指南

## 🎯 目标

将小小榆改造成一个**完全本地化、可自我训练的 AI 助手**，不依赖任何云端服务。

---

## 📋 改造完成的功能

### ✅ 阶段1：本地化（已完成）

1. **本地模型运行**
   - 集成 Ollama，支持完全离线推理
   - 已下载 Qwen2.5-0.5B 模型
   - 正在下载 DeepSeek-R1-1.5B 模型（推理能力更强）

2. **界面改造**
   - ✅ 新增"微调训练"页面入口
   - ✅ 数据集编辑器组件
   - ✅ 训练控制面板组件
   - ✅ 微调主页面整合

3. **后端支持**
   - ✅ 数据集管理 IPC（创建、编辑、导入、导出）
   - ✅ 微调任务管理 IPC
   - ✅ Python 训练脚本调用
   - ✅ 模型导出到 Ollama

4. **训练工具**
   - ✅ `python/train.py` - QLoRA 微调脚本
   - ✅ `python/setup.py` - 依赖安装脚本
   - ✅ `python/export.py` - 模型导出脚本

---

## 🚀 使用流程

### 第一步：环境准备

#### 1.1 安装 Python 依赖

```bash
cd E:\ai-chat-desktop\python
python setup.py
```

这将自动安装：
- PyTorch（CUDA 12.1）
- Transformers
- PEFT（LoRA）
- Datasets
- BitsAndBytes（量化）
- TRL（训练工具）

预计下载约 **5GB**，需要 **15-30 分钟**。

#### 1.2 验证环境

```bash
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}')"
```

应该看到：
```
PyTorch: 2.x.x
CUDA: True
```

---

### 第二步：准备训练数据

#### 2.1 在小小榆界面操作

1. 打开小小榆
2. 点击"设置" → "微调训练"
3. 点击"新建数据集"
4. 添加训练数据

#### 2.2 数据格式

每条数据包含三个字段：

```json
{
  "instruction": "你要教模型做什么（必填）",
  "input": "具体的输入内容（可选）",
  "output": "期望的输出结果（必填）"
}
```

**示例数据：**

```json
[
  {
    "instruction": "请用中文回答问题",
    "input": "什么是人工智能？",
    "output": "人工智能（AI）是计算机科学的一个分支，致力于创建能够执行通常需要人类智能的任务的系统。"
  },
  {
    "instruction": "将以下文本翻译成英文",
    "input": "你好，世界！",
    "output": "Hello, World!"
  },
  {
    "instruction": "编写一个 Python 函数计算斐波那契数列",
    "input": "",
    "output": "def fibonacci(n):\n    if n <= 1:\n        return n\n    return fibonacci(n-1) + fibonacci(n-2)"
  }
]
```

#### 2.3 数据集大小建议

- **测试训练**：100-200 条
- **实际使用**：1000-5000 条
- **专业领域**：3000-10000 条

---

### 第三步：开始微调训练

#### 3.1 在界面中操作

1. 在"微调训练"页面，点击"新建任务"
2. 选择基座模型（推荐：`deepseek-r1:1.5b`）
3. 选择数据集
4. 配置参数（使用默认值即可）
5. 点击"开始训练"

#### 3.2 训练参数说明

| 参数 | 默认值 | 说明 | RTX 4060 建议 |
|------|--------|------|---------------|
| LoRA Rank | 8 | LoRA 矩阵维度 | 8（平衡效果和速度）|
| LoRA Alpha | 16 | 学习率缩放因子 | 16 |
| Epochs | 3 | 训练轮数 | 3-5 |
| Learning Rate | 2e-4 | 学习率 | 2e-4（保持默认）|
| Batch Size | 1 | 每批样本数 | 1（固定）|
| Gradient Accumulation | 4 | 梯度累积步数 | 4-8 |

#### 3.3 训练时间估算

**RTX 4060 (8GB) 性能参考：**

| 数据集大小 | 训练时长 | 显存占用 |
|-----------|---------|---------|
| 100 条 | 10-15 分钟 | 6-7 GB |
| 500 条 | 30-60 分钟 | 6-7 GB |
| 1000 条 | 1-2 小时 | 6-7 GB |
| 5000 条 | 5-10 小时 | 6-7 GB |

**注意：**
- 训练期间显卡满载，温度会上升
- 可以继续使用电脑做其他事（CPU 任务）
- 建议在夜间或不使用电脑时训练

---

### 第四步：导出模型

#### 4.1 训练完成后

1. 在"任务列表"中找到已完成的任务
2. 点击"导出到 Ollama"
3. 等待导出完成（约 1-2 分钟）

#### 4.2 验证模型

在命令行测试：

```bash
ollama list
# 应该看到你的模型名称

ollama run 你的模型名称
# 测试对话
```

#### 4.3 在小小榆中使用

1. 返回聊天界面
2. 切换模型 → 选择你的微调模型
3. 开始对话，测试效果

---

## 📊 效果对比

### 基座模型 vs 微调模型

| 维度 | 基座模型 | 微调后 |
|------|---------|--------|
| 通用知识 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 专业领域 | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| 回答风格 | 通用 | 定制化 |
| 特定任务 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**示例场景：**

假设你微调了一个"编程助手"：

- ❌ **基座模型**：会写代码，但风格不统一，可能用你不熟悉的库
- ✅ **微调模型**：严格遵循你的代码规范，使用你项目中的技术栈，注释风格一致

---

## 💡 最佳实践

### 数据集制作技巧

#### 1. 质量 > 数量
- 100 条高质量数据 > 1000 条低质量数据
- 每条数据都要人工审核
- 输出要准确、完整、符合期望

#### 2. 覆盖多种场景
```
领域：编程助手
├─ 代码生成（40%）
├─ 代码解释（30%）
├─ Bug 修复（20%）
└─ 代码优化（10%）
```

#### 3. 包含反例
```json
{
  "instruction": "编写一个排序函数",
  "input": "需求：性能要求高",
  "output": "建议使用快速排序...\n\n❌ 不要使用冒泡排序（性能差）"
}
```

#### 4. 统一格式
- 所有输出使用相同的 Markdown 格式
- 代码块统一用 ```language
- 术语翻译保持一致

---

### 模型选择建议

#### 轻量级（日常使用）
- **Qwen2.5-1.5B**：极速响应，适合简单任务
- **DeepSeek-R1-1.5B**：推理能力强，支持思维链

#### 平衡型（推荐）⭐
- **Qwen2.5-7B**：中文最强开源模型
- **DeepSeek-R1-Distill-7B**：继承 DeepSeek V4 推理能力

#### 重型（高质量）
- **Qwen2.5-14B**：接近 GPT-3.5 水平（需要量化）

**RTX 4060 最佳选择：DeepSeek-R1-Distill-7B**

---

## 🛠️ 技术细节

### QLoRA 原理

```
完整模型：7B 参数 × 4 字节 = 28 GB（装不下）
                    ↓
4bit 量化：7B 参数 × 0.5 字节 = 3.5 GB（可以装下）
                    ↓
只训练 LoRA：额外 8M-64M 参数（几乎不占显存）
```

**QLoRA = 量化（4bit）+ LoRA（低秩适配）**

### 为什么不会"学坏"？

1. **基座知识保留**：LoRA 只修改少量参数（< 1%）
2. **领域专精**：在特定领域变强，不影响其他能力
3. **可逆性**：删除 LoRA 权重即恢复原模型

### 训练日志位置

```
E:\ai-chat-desktop\
├─ python\
│   ├─ train.py          # 训练脚本
│   ├─ setup.py          # 环境安装
│   └─ export.py         # 模型导出
│
├─ datasets\             # 数据集导出
│   └─ dataset_1.json
│
└─ finetune\             # 训练输出
    └─ task_1\
        ├─ checkpoint-100\
        ├─ checkpoint-200\
        ├─ merged\        # 最终模型（这个导入 Ollama）
        └─ metadata.json  # 训练元数据
```

---

## ❓ 常见问题

### Q1: 训练中显存不足怎么办？

**解决方案：**
1. 降低 LoRA Rank（8 → 4）
2. 减少序列长度（512 → 256）
3. 使用更小的基座模型（7B → 3B）

### Q2: 训练后效果不好？

**可能原因：**
1. ❌ 数据量太少（< 100 条）
2. ❌ 数据质量差（格式不统一、有错误）
3. ❌ 训练轮数不够（Epochs < 3）
4. ❌ 学习率不合适

**改进方法：**
1. ✅ 增加数据到 500-1000 条
2. ✅ 人工审核每条数据
3. ✅ 增加 Epochs 到 5-10
4. ✅ 调整学习率（1e-4 ~ 5e-4）

### Q3: 可以多次微调吗？

**可以！**

```
基座模型
  ↓ 第一次微调（编程能力）
小小榆-v1
  ↓ 第二次微调（中文写作）
小小榆-v2
  ↓ 第三次微调（领域知识）
小小榆-v3
```

每次微调都在上次基础上进行。

### Q4: 训练数据会泄露吗？

**不会！**

- ✅ 完全本地训练
- ✅ 数据不上传任何服务器
- ✅ 模型只在你的电脑上

---

## 🎓 进阶玩法

### 1. 多模型组合

```
小小榆-通用版（日常聊天）
小小榆-编程版（写代码）
小小榆-写作版（文章、报告）
小小榆-翻译版（中英互译）
```

根据任务切换不同模型。

### 2. 持续学习

每天收集对话中的优质回答：
1. 点击"添加到数据集"
2. 积累 100 条后重新微调
3. 模型不断进化

### 3. 领域专家

**医疗助手示例：**
- 收集 5000 条医学问答
- 基于 Qwen2.5-7B 微调
- 得到专业的医学咨询模型

---

## 📈 性能优化

### 加速训练

1. **使用更好的硬件**
   - RTX 4060 → RTX 4070（12GB）
   - 训练速度提升 50%

2. **优化参数**
   ```python
   gradient_accumulation_steps=8  # 4 → 8
   max_length=256                 # 512 → 256
   ```

3. **分阶段训练**
   - 第一轮：Epochs=1，快速验证
   - 第二轮：Epochs=5，精细训练

---

## 🔒 隐私保护

### 为什么本地训练更安全？

| 维度 | 云端 API | 本地训练 |
|------|---------|---------|
| 数据上传 | ✅ 是 | ❌ 否 |
| 模型所有权 | 🔒 服务商 | ✅ 你自己 |
| 使用限制 | ✅ 有限额 | ❌ 无限 |
| 成本 | 💰 按量付费 | 🆓 一次投入 |
| 离线使用 | ❌ 不可 | ✅ 可以 |

---

## 📦 文件结构

```
E:\ai-chat-desktop\
├─ src\
│   ├─ renderer\
│   │   ├─ pages\
│   │   │   └─ Finetune.tsx           # 微调主页面
│   │   ├─ components\
│   │   │   └─ finetune\
│   │   │       ├─ DatasetEditor.tsx  # 数据集编辑器
│   │   │       └─ TrainingPanel.tsx  # 训练面板
│   │   └─ api\
│   │       └─ finetune.ts            # 前端 API
│   └─ main\
│       └─ ipc\
│           └─ finetune.ipc.ts        # 后端 IPC
│
├─ python\                            # Python 训练脚本
│   ├─ train.py                       # 微调训练
│   ├─ setup.py                       # 环境安装
│   └─ export.py                      # 模型导出
│
├─ datasets\                          # 数据集存储
└─ finetune\                          # 训练输出
```

---

## 🎉 总结

### 已完成的功能

✅ **阶段1：本地化**
- Ollama 集成
- DeepSeek-R1 模型下载
- 完全离线推理

✅ **阶段2：微调训练**
- 数据集编辑器
- QLoRA 训练脚本
- 模型导出到 Ollama
- 完整的界面集成

### 接下来可以做

📌 **阶段3：进阶功能**
- [ ] 模型性能对比工具
- [ ] 自动数据增强
- [ ] 训练进度可视化
- [ ] 模型版本管理
- [ ] 一键分享模型

---

## 🆘 获取帮助

### 遇到问题？

1. **查看日志**：`finetune/task_X/` 目录
2. **检查环境**：运行 `python setup.py`
3. **验证数据**：确保 JSON 格式正确

### 常见错误

```
❌ CUDA out of memory
→ 降低 LoRA Rank 或使用更小的模型

❌ 找不到 Python
→ 确认 Python 已安装并在 PATH 中

❌ 导入模型失败
→ 检查 Ollama 是否运行（ollama list）
```

---

## 📚 参考资源

- **QLoRA 论文**：https://arxiv.org/abs/2305.14314
- **PEFT 文档**：https://huggingface.co/docs/peft
- **Ollama 文档**：https://ollama.ai/docs
- **DeepSeek 开源**：https://github.com/deepseek-ai

---

**🎊 恭喜！你现在拥有了一个完全属于自己的、可以不断进化的 AI 助手！**
