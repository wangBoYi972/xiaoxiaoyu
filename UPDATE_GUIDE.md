# 小小榆桌面应用更新指南

**更新日期：** 2026-08-12  
**版本：** 3.0.0 → 3.1.0

---

## 🎉 本次更新内容

### ✨ 新增功能

1. **DeepSeek-R1 模型支持**
   - 已下载 deepseek-r1:1.5b (1.1 GB)
   - 已下载 deepseek-r1:7b (4.7 GB)
   - 推理能力更强，支持思维链

2. **模型微调功能**
   - 数据集编辑器（创建、导入、导出）
   - 训练控制面板（配置参数、开始训练）
   - 训练进度显示
   - 模型导出到 Ollama

3. **测试数据集**
   - 预置 10 条高质量测试数据
   - 覆盖多种场景（问答、代码、优化等）

4. **Python 训练脚本**
   - QLoRA 微调（适配 RTX 4060 8GB）
   - 自动环境检查
   - 模型导出工具

### 📚 新增文档

- `OPTIMIZATION_PLAN.md` - 优化计划和待办事项
- `QUICKSTART_5MIN.md` - 5分钟快速上手指南
- `test_training.py` - 命令行快速测试脚本

---

## 🚀 立即更新（两种方法）

### 方法 1：开发模式（推荐先测试）

**适用场景：** 测试新功能，实时看到代码更新

**步骤：**

1. **双击运行**
   ```
   E:\ai-chat-desktop\启动开发版.bat
   ```

2. **等待启动**
   - Vite 开发服务器启动（约 5 秒）
   - Electron 窗口自动打开（约 10 秒）

3. **测试新功能**
   - 点击右上角"设置"图标 ⚙️
   - 点击"模型微调"按钮
   - 查看微调训练界面

**优点：**
- ✅ 启动快（不需要打包）
- ✅ 实时更新（修改代码立即生效）
- ✅ 便于调试

**缺点：**
- ❌ 需要保持命令行窗口开启
- ❌ 关闭窗口后需要重新运行脚本

---

### 方法 2：打包安装（用于日常使用）

**适用场景：** 正式使用，像普通软件一样启动

**步骤：**

1. **双击运行**
   ```
   E:\ai-chat-desktop\打包应用.bat
   ```

2. **等待打包完成**
   - 清理旧构建（1 秒）
   - 构建应用（约 1-2 分钟）
   - 打包为安装程序（约 2-3 分钟）

3. **安装新版本**
   ```
   打开：E:\ai-chat-desktop\release\
   运行：XiaoXiaoYu-Setup-3.0.0.exe
   ```

4. **安装选项**
   - 如果已安装旧版：直接覆盖安装
   - 如果是首次安装：选择安装位置

5. **启动应用**
   - 双击桌面图标
   - 或从开始菜单打开

**优点：**
- ✅ 独立应用（不需要命令行）
- ✅ 开机自启动（可选）
- ✅ 像普通软件一样使用

**缺点：**
- ❌ 打包耗时（3-5 分钟）
- ❌ 每次更新都需要重新打包

---

## 🧪 测试新功能

### 1. 测试微调界面

启动应用后：

1. 点击右上角 **⚙️ 设置**
2. 点击 **"模型微调"** 按钮（蓝色）
3. 应该看到三个标签页：
   - 📊 数据集编辑
   - ⚡ 微调训练
   - 🚀 模型管理（待开发）

### 2. 导入测试数据集

在"数据集编辑"标签页：

1. 点击 **"导入 JSON"**
2. 选择文件：
   ```
   E:\ai-chat-desktop\datasets\example_dataset.json
   ```
3. 输入名称：`测试数据集`
4. 点击 **"保存"**

### 3. 测试训练功能（命令行）

由于界面的训练功能目前是模拟的，建议先用命令行测试：

```bash
cd E:\ai-chat-desktop
python test_training.py
```

这会：
- ✅ 检查 Python 环境
- ✅ 检查 Ollama 模型
- ✅ 配置训练参数
- 🔥 真实训练（约 5-10 分钟）
- 📦 导出到 Ollama

### 4. 测试微调后的模型

训练完成后：

```bash
ollama run xiaoxiaoyu-test
```

输入测试问题：
```
>>> 介绍你自己
>>> 写一个 Python 函数计算两个数的和
>>> 解释什么是大模型微调
```

---

## 📋 完整的更新和测试流程

### 第一次更新（推荐流程）

```
1. 双击 "启动开发版.bat" 
   → 测试界面是否正常
   
2. 测试微调界面
   → 确认新功能可见
   
3. 安装 Python 依赖
   → python python\setup.py
   
4. 命令行测试训练
   → python test_training.py
   
5. 测试微调后的模型
   → ollama run xiaoxiaoyu-test
   
6. 如果一切正常，打包正式版本
   → 双击 "打包应用.bat"
   
7. 安装新版本
   → release\XiaoXiaoYu-Setup-3.0.0.exe
```

### 日常使用

```
1. 双击桌面图标启动小小榆
2. 使用微调功能
3. 定期更新模型
```

---

## ❓ 常见问题

### Q1: 启动开发版失败

**错误：** `npm: command not found`

**解决：**
```bash
# 检查 Node.js 是否安装
node --version

# 如果未安装，去官网下载：
# https://nodejs.org/
```

### Q2: 打包失败

**错误：** `electron-builder not found`

**解决：**
```bash
cd E:\ai-chat-desktop
npm install
```

### Q3: 找不到 Python

**错误：** `python: command not found`

**解决：**
```bash
# 检查 Python 是否在 PATH 中
python --version

# 或使用完整路径
D:\php\python.exe test_training.py
```

### Q4: 开发版和正式版冲突

**现象：** 两个版本同时运行

**解决：**
1. 关闭所有小小榆窗口
2. 打开任务管理器
3. 结束所有 `electron.exe` 进程
4. 重新启动

---

## 🔄 回退到旧版本

如果新版本有问题，可以回退：

### 方法 1：使用旧的安装包

```
1. 找到旧版安装包
2. 卸载新版本
3. 安装旧版本
```

### 方法 2：从备份恢复

```
1. 复制备份目录覆盖当前目录
2. 重新打包
```

---

## 📊 性能对比

| 操作 | 开发模式 | 打包模式 |
|------|---------|---------|
| 首次启动 | 15 秒 | 3 秒 |
| 内存占用 | 200-300 MB | 150-200 MB |
| CPU 占用 | 低 | 低 |
| 适用场景 | 开发/测试 | 日常使用 |

---

## 📝 下一步

- [ ] 测试开发版
- [ ] 导入测试数据集
- [ ] 命令行测试训练
- [ ] 打包正式版本
- [ ] 制作自己的数据集
- [ ] 进行完整训练

---

## 🆘 需要帮助？

遇到问题时：

1. **查看日志**
   - 开发模式：查看命令行输出
   - 打包模式：查看 `%APPDATA%\小小榆\logs\`

2. **检查环境**
   ```bash
   node --version
   python --version
   ollama list
   ```

3. **重新构建**
   ```bash
   cd E:\ai-chat-desktop
   npm install
   npm run build
   ```

---

**🎊 祝你更新顺利！开始体验全新的微调功能！**
